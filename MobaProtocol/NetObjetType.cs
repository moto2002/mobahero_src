using System;

namespace MobaProtocol
{
	public enum NetObjetType
	{
		TUnknow,
		TCustomObj,
		TORMObj,
		TARRAY,
		TDictionary,
		TList,
		TVector3,
		TBool,
		TByte,
		TByteList,
		TInt,
		TLong,
		TShort,
		TFloat,
		TDouble,
		TString,
		TNull
	}
}
